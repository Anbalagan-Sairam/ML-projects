# ML-projects

This repository contains a curated list of machine learning projects.

Structure suggestions:
- `notebooks/` — exploratory notebooks
- `src/` — reusable code and modules
- `data/` — datasets (keep large data out of repo)
- `models/` — trained model artifacts

Contributions welcome.
